// Se define la función greet 
function greet(name: string): string {
    return `Hola, bienvenido: ${name}`;
}
  
// Se llama a la función
const greetMessage = greet("Ezequiel");
console.log(greetMessage);
  